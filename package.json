{
  "name": "absolute-protagonist-desktop-pet",
  "version": "0.1.0",
  "description": "A transparent Windows desktop pet powered by local Absolute protagonist WebM assets.",
  "private": true,
  "scripts": {
    "start": "tauri dev",
    "dev": "tauri dev",
    "package:win": "npm run package:installer",
    "package:installer": "node ./scripts/package-installer.js"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
